declare const _default: {
    'AnalyticsBrowser should return the correct type': () => void;
    'AnalyticsBrowser should return the correct type if awaited on.': () => Promise<void>;
    'Promise API should work': () => void;
    'If catch is before "then" in the middleware chain, .then should take into account the catch clause': () => void;
    'Group should have the correct type': () => void;
    'User should have the correct type': () => void;
    'Identify should work with spread objects ': () => void;
    'Track should work with spread objects': () => void;
    'Identify should work with generic objects ': () => void;
    'Context should have a key allowing arbitrary properties': () => Promise<void>;
    'Track should allow undefined properties': () => void;
    'Lazy instantiation should be supported': () => void;
    '.load should return this': () => void;
    'Should accept traits': () => void;
    'Should accept optional ExtraContext': () => void;
};
/**
 * These are general typescript definition tests;
 * They aren't meant to be run by anything but the typescript compiler.
 */
export default _default;
//# sourceMappingURL=analytics-browser.d.ts.map